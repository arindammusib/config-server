spring:
  application:
    name: configserver

  profiles:
    active: git

  cloud:
    config:
      server:
        git:
          uri: https://github.com/arindammusib/config-server
          default-label: main
          clone-on-start: true
          force-pull: true
          timeout: 5

server:
  port: 8071